{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug active CMake target",
      "type": "cppvsdbg",            // use cppdbg + gdb if you are on MinGW
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "args": [],
      "console": "integratedTerminal",
      "preLaunchTask": "CMake: build"
    }
  ]
}